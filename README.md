# securepapp
 SecureApp is an Android application that provides secure authentication features, including two-factor authentication (2FA), password recovery, and profile management. The app uses SQLite for data storage, SHA-256 for password hashing, and implements input validation and error handling.
